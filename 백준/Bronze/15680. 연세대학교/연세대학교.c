#include <stdio.h>

int main(void) {
    int input;
    scanf("%d",&input);
    if (input==1) {
        printf("Leading the Way to the Future");
    }
    if (input==0) {
        printf("YONSEI");
    }
    return 0;
}